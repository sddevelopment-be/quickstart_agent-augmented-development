# Environment variables for Docker container
# Copy this file to .env and customize for your environment

# GitHub Personal Access Token for API access
# Generate at: https://github.com/settings/tokens
# Required permissions: repo (for private repos), workflow (for CI/CD)
GITHUB_TOKEN=your_github_token_here

# Optional: Python path configuration
# PYTHONPATH=/workspace/framework:/workspace/ops

# Optional: Custom working directory
# WORKDIR=/workspace

# Note: Never commit .env file with actual tokens to version control
# The .env file should be in .gitignore
