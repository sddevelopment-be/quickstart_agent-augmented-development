sonar.projectKey=sddevelopment-be_quickstart_agent-augmented-development
sonar.organization=sddevelopment-be

# This is the name and version displayed in the SonarCloud UI.
#sonar.projectName=quickstart_agent-augmented-development
#sonar.projectVersion=1.0

# Source directories
sonar.sources=src,framework

# Test directories
sonar.tests=tests

# Python-specific settings
sonar.python.version=3.10

# Coverage report path (generated by pytest-cov)
sonar.python.coverage.reportPaths=coverage.xml

# Exclusions
sonar.exclusions=**/node_modules/**,**/htmlcov/**,**/__pycache__/**,**/*.pyc,**/output/**,**/tmp/**,**/work/**,**/telemetry.db

# Encoding of the source code
sonar.sourceEncoding=UTF-8