---
name: "Work Directory Orchestration Approach"
description: "Provide a single, precise reference for the file-based asynchronous orchestration model that powers the `work/` directory. The approach keeps exploratory and operational work visible inside Git by ..."
version: "1.0.0"
type: "approach"
category: "coordination-pattern"
---

# Work Directory Orchestration Approach

Provide a single, precise reference for the file-based asynchronous orchestration model that powers the `work/` directory. The approach keeps exploratory and operational work visible inside Git by ...

## Instructions

Provide a single, precise reference for the file-based asynchronous orchestration model that powers the
`work/` directory. The approach keeps exploratory and operational work visible inside Git by representing every task as a YAML file that flows through clearly defined directories and lifecycle states. Humans, agents, and automation can collaborate without bespoke services while preserving a full audit trail.

